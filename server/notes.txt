to install the virtual environment you need to install virtualenv
py -m install virtualenv

To create a new virtualenv
-py -m venv venv

To activate the virtualenv
venv\scripts\activate

-mac
source ven/bin/activate

-to check python version
Windows- py --version
Mac-  python3 --version

--To run python server
py server.py (not the best way)

--cursor is a dumbed down version of a list

--REST API
REST tools => REST API

{} = a  dictionary in python
[] = a list in python

CORS = cross orgin resource sharing

##arrow functions